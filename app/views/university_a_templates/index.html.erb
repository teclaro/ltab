<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All university as
    </h1>

    <a href="/university_as/new" class="btn btn-block btn-outline-secondary">
      Add a new university a
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @university_as.each do |university_a| %>
      <tr>
        <!-- Display university_a.id start -->
        <td>
          <%= university_a.id %>
        </td>
        <!-- Display university_a.id end -->

        <!-- Display university_a.created_at start -->
        <td>
          <%= time_ago_in_words(university_a.created_at) %> ago
        </td>
        <!-- Display university_a.created_at end -->

        <!-- Display university_a.updated_at start -->
        <td>
          <%= time_ago_in_words(university_a.updated_at) %> ago
        </td>
        <!-- Display university_a.updated_at end -->

        <td>
          <a href="/university_as/<%= university_a.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @university_as, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
